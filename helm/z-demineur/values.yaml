global:
  docker:
    # Artifactory host used for download Docker images
    artifactoryUrl: "zelytra"
    # Set in Gitlab-ci at the deployment. See values in CI/CD gitlab variables
    user: ""
    password: ""
    imageTag:
      zdemineurFrontend: latest
      zdemineurBackend: latest

  config:
    PLATFORM: dv1