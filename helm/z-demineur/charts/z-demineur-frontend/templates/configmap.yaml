apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "z-demineur-frontend.fullname" $ }}-env
  labels:
    app: {{ template "z-demineur-frontend.fullname" $ }}
    chart: {{ template "z-demineur-frontend.chart" $ }}
    heritage: {{ $.Release.Service }}
    release: {{ $.Release.Name }}
data:
  PLATFORM: {{ $.Values.global.config.PLATFORM | quote }}
  APP_NAME: {{ $.Chart.Name | quote }}
  LOGGER_NAME: {{ $.Values.config.LOGGER_NAME | quote }}

  VITE_SERVER_URL: ""