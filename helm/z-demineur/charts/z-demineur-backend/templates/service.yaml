apiVersion: v1
kind: Service
metadata:
  name: {{ template "z-demineur-backend.fullname" . }}
  labels:
    app: {{ template "z-demineur-backend.fullname" $ }}
    chart: {{ template "z-demineur-backend.chart" $ }}
    heritage: {{ $.Release.Service }}
    release: {{ $.Release.Name }}
    platform: {{ .Values.global.config.PLATFORM }}
spec:
  ports:
    - name : http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    app: {{ template "z-demineur-backend.fullname" $ }}
    release: {{ .Release.Name }}